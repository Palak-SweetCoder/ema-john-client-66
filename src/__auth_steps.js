/**
 * 1. create a new project in console.firebase.google.com 
 * 2. npm install firebase
 * 3. Copy the codes from firebase and create a new file in src folder firebase.init.js 
 * 4. Firebase settings > authentication > enable email and password auth
*/


/**
 * Hosting steeps
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. firebase init
 * 4. firebase deploy
*/